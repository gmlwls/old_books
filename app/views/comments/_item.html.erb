<li>  
	<div id="comment-<%=comment.id%>">
    <strong><%= comment.user.username %></strong> : <%= comment.content %>
    <% if comment.user == current_user %>
    <%= link_to "수정", edit_book_comment_path(book, comment), remote: true %>
    <%= link_to "삭제", book_comment_path(book, comment), remote: true, method: :delete %>
    <% end %>
    </div>
</li>  