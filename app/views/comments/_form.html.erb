<%= form_for [@comment.book, @comment], remote: true do |f| %>
  	 <strong><%= @comment.user.username %></strong> : <%= f.text_field :content, :autocomplete => :off %>
   	 <%= f.submit %>
<% end %>