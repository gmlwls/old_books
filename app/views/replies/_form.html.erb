<%= form_for [@book, @comment, @comment.replies.new] do |f| %>
	<% comment_id = @comment.id%>
	<%= f.text_field :content, class: "repl_content", id: "reply_content-#{comment_id}", :autocomplete => :off,:required => true %>
	<%= f.submit "작성", class:"repl" %>
<% end %>