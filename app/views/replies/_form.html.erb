<%= form_for [@book, @comment, @comment.replies.new] do |f| %>
	<% comment_id = @comment.id%>
	<%= f.text_field :content, id: "reply_content-#{comment_id}", :autocomplete => :off %>
	<%= f.submit "작성" %>
<% end %>