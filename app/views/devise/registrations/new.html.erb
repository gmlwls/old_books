<style>
  .container{
    margin: 100px;
  }
  h2 {
    font-size: 1.2rem;
    font-weight: bold;
    color: gray;
  }
  .field{
    margin: 10px 0;
  }
  label{
    margin-bottom: 3px;
  }
  .field > input{
    width: 100%;
    height: 30px;
  }
  .actions{
    margin-bottom: 10px;
  }
</style>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
    <h2><%= t('.sign_up') %></h2><hr>


<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  
  <div class="field">
    <%= f.label :이름 %><br />
    <%= f.text_field :username, autofocus: true, autocomplete: "username", required: true %>
  </div>
  
  <div class="field">
    <%= f.label :학과 %><br />
    <%= f.text_field :school, autofocus: true, autocomplete: "school", required: true, placeholder: "예) 경영학과" %>
    
  </div>

  <div class="field">
    <%= f.label :학번 %><br />
    <%= f.text_field :studentid, autofocus: true, autocomplete: "studentid", required: true, placeholder: "14학번이시라면 '14'라고 숫자만 기입" %>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit t('.sign_up') %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
  </div>
</div>
